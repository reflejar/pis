apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pis.fullname" . }}-test-connection"
  labels:
    {{- include "pis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
